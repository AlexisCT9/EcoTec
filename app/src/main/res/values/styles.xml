<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 🌿 Estilo personalizado para los diálogos de EcoTec -->
    <style name="CustomDialog" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="android:background">@android:color/white</item>
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:windowElevation">8dp</item>
        <item name="shapeAppearanceOverlay">@style/DialogShape</item>
    </style>

    <!-- Bordes redondeados del diálogo -->
    <style name="DialogShape" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>

</resources>
